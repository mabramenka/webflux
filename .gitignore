.gradle/
build/

.idea/
*.iml

out/

# Local AI/tooling notes and profiles. Keep the files in their default lookup paths,
# but do not treat them as project artifacts.
.claude/
CLAUDE.md
docs/project-review.md

# Dev-only TLS material (see scripts/gen-dev-certs.sh). Never commit key material.
src/main/resources/certs/
