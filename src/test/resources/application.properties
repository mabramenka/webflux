# Tests run without the `https` default profile so that integration tests can talk to
# plain-HTTP reactor-netty fixtures without provisioning certificates. Overriding only
# `spring.profiles.default` keeps every other setting (actuator exposure, reactor
# context propagation, HTTP client defaults) inherited from application.yaml.
spring.profiles.default=test
